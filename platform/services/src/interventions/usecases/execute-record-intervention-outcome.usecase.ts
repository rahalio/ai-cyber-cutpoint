/**
 * ExecuteRecordInterventionOutcome - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: interventions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRecordInterventionOutcome Use Case
 *
 * DDD: Application use case for get Outcome.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RecordInterventionOutcomeInput, RecordInterventionOutcomeOutput } from "../dto/outcome.dto";
import type { ExecutionContextService, IdGeneratorService } from "@cutpoint/services/_shared/index.js";
import type { OutcomeRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRecordInterventionOutcome {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly outcome: OutcomeRepository
  ) {}

  async execute(input: RecordInterventionOutcomeInput): Promise<RecordInterventionOutcomeOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.itvId();

    // All field names extracted from OpenAPI spec: harmMetric, notes, substitutionObserved, value, windowEnd, windowStart
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).interventionId) {
      throw new ValidationError("interventionId is required");
    }
    const interventionId = (input as any).interventionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.outcome.recordInterventionOutcome({ interventionId: interventionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${interventionId}`);
    }

    return entity as RecordInterventionOutcomeOutput;
  }
}
