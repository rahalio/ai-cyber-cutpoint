/**
 * GetEvidencePackage handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: governance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getEvidencePackage } from "@cutpoint/api-server/domains/governance/handlers";
import type { GovernanceDDDDependencies } from "@cutpoint/api-server/domains/governance";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@cutpoint/tests/shared";
import { GOVERNANCE_USE_CASES } from "../__factories__/use-cases.js";

describe("GetEvidencePackage", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: GovernanceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<GovernanceDDDDependencies>(
      GOVERNANCE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetEvidencePackage, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { packageId: entity.id };

    await getEvidencePackage(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetEvidencePackage.execute).toHaveBeenCalledWith(expect.objectContaining({ packageId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { packageId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetEvidencePackage, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getEvidencePackage(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
