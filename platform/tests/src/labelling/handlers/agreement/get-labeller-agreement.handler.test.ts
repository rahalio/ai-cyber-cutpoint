/**
 * GetLabellerAgreement handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: labelling
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getLabellerAgreement } from "@cutpoint/api-server/domains/labelling/handlers";
import type { LabellingDDDDependencies } from "@cutpoint/api-server/domains/labelling";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@cutpoint/tests/shared";
import { LABELLING_USE_CASES } from "../__factories__/use-cases.js";

describe("GetLabellerAgreement", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: LabellingDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<LabellingDDDDependencies>(
      LABELLING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetLabellerAgreement, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { batchId: entity.id };

    await getLabellerAgreement(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetLabellerAgreement.execute).toHaveBeenCalledWith(expect.objectContaining({ batchId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { batchId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetLabellerAgreement, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getLabellerAgreement(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
